mutation updateTermsAndConditions($userProfile: UserProfileInput!) {
    updateCurrentUserProfile(userProfile: $userProfile) {
        ... on User {
            termsAndConditionsId
            shouldUpdateTnC
        }
    }
}
