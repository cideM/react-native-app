query AccessList{
  currentUser {
    mobileAccessList{
      ... on MobileAccessError{
        __typename
        target
        errorCode
      }
      ... on MobileAccess{
        __typename
        target
        offlineExpiryDate
      }
    }
  }
}
